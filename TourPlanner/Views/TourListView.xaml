<UserControl x:Class="TourPlanner.Views.TourListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="TourList" ItemsSource="{Binding TourDetails}" Background="LightGray" SizeChanged="TourListView_SizeChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding description}"/>
                    <GridViewColumn Header="From" DisplayMemberBinding="{Binding from}"/>
                    <GridViewColumn Header="To" DisplayMemberBinding="{Binding to}"/>
                    <GridViewColumn Header="Distance" DisplayMemberBinding="{Binding distance}"/>
                    <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding duration}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
